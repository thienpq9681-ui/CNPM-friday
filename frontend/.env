# Frontend Configuration

# API Configuration - Must connect to Backend
# Inside Docker: use service name 'backend' from docker-compose
# Outside Docker: use http://localhost:8000
VITE_API_URL=http://backend:8000/api/v1

# Debug Mode (optional)
VITE_DEBUG=false
